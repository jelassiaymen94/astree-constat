import{useEffect,useRef,useState}from'react';import{getClaimEmails,sendClaimEmail,type EmailLog}from'./emailApi';import type{Generation}from'./types';import'./email.css';
const escapeHtml=(v:string)=>v.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');const toHtml=(v:string)=>v.split(/\n{2,}/).map(p=>`<p>${escapeHtml(p).replace(/\n/g,'<br>')}</p>`).join('');
export default function EmailComposer({claimId,generation,customerName,customerId}:{claimId:string;generation:Generation;customerName:string;customerId:string}){const editor=useRef<HTMLDivElement>(null);const[subject,setSubject]=useState(`Suivi de votre sinistre ${claimId}`),[html,setHtml]=useState(()=>toHtml(generation.generatedContent||'')),[preview,setPreview]=useState(false),[sending,setSending]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState(''),[emails,setEmails]=useState<EmailLog[]>([]);const recipient=`${customerId.toLowerCase()}@demo.astree.local`;
useEffect(()=>{const next=toHtml(generation.generatedContent||'');setHtml(next);if(editor.current)editor.current.innerHTML=next},[generation.generationId]);useEffect(()=>{getClaimEmails(claimId).then(setEmails).catch(()=>{})},[claimId]);function format(c:string){document.execCommand(c);editor.current?.focus();setHtml(editor.current?.innerHTML||'')}
async function send(){setError('');setMessage('');if(!subject.trim()||!html.trim()){setError('L’objet et le contenu sont obligatoires.');return}if(!window.confirm(`Confirmer l’envoi de cet e-mail de démonstration pour ${customerName} ?`))return;setSending(true);try{const sent=await sendClaimEmail(claimId,{clientRequestId:crypto.randomUUID(),generationId:generation.generationId,subject:subject.trim(),bodyHtml:html,confirmation:true});setEmails(c=>[sent,...c]);setMessage(`E-mail envoyé avec succès dans Mailtrap (${sent.actualRecipientEmail}).`)}catch(e){setError((e as Error).message)}finally{setSending(false)}}
return <section className="emailComposer"><div className="emailTitle"><div><small>E-MAIL DE DÉMONSTRATION</small><h2>Préparer l’e-mail à l’assuré</h2><p>Le message final utilise un modèle HTML ASTREE compatible avec les clients e-mail.</p></div><span>Mailtrap Sandbox</span></div><div className="emailFields"><label>Destinataire<input value={`${customerName} — ${recipient}`} readOnly/></label><label>Objet<input value={subject} onChange={e=>setSubject(e.target.value)} maxLength={200}/></label></div><div className="editorBox"><div className="toolbar"><button type="button" onClick={()=>format('bold')}><b>G</b></button><button type="button" onClick={()=>format('italic')}><i>I</i></button><button type="button" onClick={()=>format('underline')}><u>S</u></button><button type="button" onClick={()=>format('insertUnorderedList')}>• Liste</button><button type="button" onClick={()=>format('undo')}>↶</button><button type="button" onClick={()=>format('redo')}>↷</button></div><div ref={editor} className="wysiwyg" contentEditable suppressContentEditableWarning onInput={e=>setHtml(e.currentTarget.innerHTML)} dangerouslySetInnerHTML={{__html:html}}/></div><div className="emailActions"><button type="button" className="secondaryAction" onClick={()=>setPreview(!preview)}>{preview?'Fermer l’aperçu':'Prévisualiser le design'}</button><button type="button" className="sendAction" disabled={sending} onClick={send}>{sending?'Envoi en cours…':'Envoyer vers Mailtrap'}</button></div>{preview&&<div className="mailCanvas"><div className="mailCard"><header><b>ASTREE</b><span>ASSURANCES</span></header><main><small>SUIVI DE DOSSIER</small><h1>{subject}</h1><p className="ref">Référence : <b>{claimId}</b></p><article dangerouslySetInnerHTML={{__html:html}}/><aside><b>Besoin d’informations complémentaires ?</b><br/>Notre équipe reste disponible pour vous accompagner dans le suivi de votre dossier.</aside><p>Cordialement,<br/><b>Service Sinistres — ASTREE Assurances</b></p></main><footer>Message préparé avec assistance rédactionnelle et validé par un gestionnaire.<br/>Démonstration ASTREE Claims AI — aucun envoi automatique.</footer></div></div>}{message&&<p className="emailSuccess">✓ {message}</p>}{error&&<p className="emailError">{error}</p>}<div className="emailHistory"><h3>Historique des e-mails</h3>{emails.length?emails.map(i=><div key={i.emailId}><span className={i.status}>{i.status==='sent'?'Envoyé':i.status==='failed'?'Échec':'En cours'}</span><b>{i.subject}</b><small>{new Intl.DateTimeFormat('fr-TN',{dateStyle:'medium',timeStyle:'short'}).format(new Date(i.createdAt))}</small></div>):<p>Aucun e-mail envoyé pour ce dossier.</p>}</div></section>}
